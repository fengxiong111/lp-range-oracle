name: Build Dist
on:
  push:
    branches: [main]
    paths-ignore: ['dist/**']
  workflow_dispatch:
permissions:
  contents: write
jobs:
  bundle:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Bundle request runtime
        run: |
          rm -rf dist
          npx --yes esbuild@0.25.10 runner.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/runner.mjs
          test -s dist/runner.mjs
      - name: Commit immutable dist
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -f dist/runner.mjs
          git diff --cached --quiet && exit 0
          git commit -m 'build: refresh prebundled oracle runtime [skip ci]'
          git push
